import 'package:flutter/material.dart';

class InformatePage extends StatelessWidget {
  const InformatePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'Contenido de Informate',
        style: TextStyle(color: Colors.white),
      ),
    );
  }
}
